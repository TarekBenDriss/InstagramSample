package instagram.mpsdm.com.instagram.models
//class Message()

data class Message(var nom: String, var message: String)
{
  //  private var nom: String=""
   // var message: String=""
    //constructor(name: String) : this(nom, "")
    //constructor(yearOfBirth: String, name: String)
        //    : this(name, 2018 - yearOfBirth)

/*
    fun turnOn(nom:String) {
       this.nom=nom
    }
*/
}